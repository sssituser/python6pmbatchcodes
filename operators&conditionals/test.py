'''
input() : It is a predefined input function, which can be used
display stmt and can read a string
print(): It is a predefined output function,which can  display the stmts
and values on the console(command line app or CUI).
                            Develop application
                        software                 web        mobile
                     Desktop or  windows or GUI
                     command or  Console or CUI   
type caste functions :
    Type Casting : Conversion one data type to another data.
int()   : This function can convert string Integer(Numbers without decimal values).
    
a = "10"        x = int(a)   x = 10
y = 'abc'       p = int(y)   


float() : Numbers with decimal values is float,  which converts string into 
       float value.
bool()
str()
complex()
type() : It is a function which can be used to check the type of data present
in the variable.

dir() : It is a functions which provides functions inforatmion of the given class
                        Operators
                        Arithmetic operators
                        Comparision operators
                        Assignment operators
                        Logical operators
                        Identity Operators
                        Membership operators
    Conditional statements : simple if 
    if  con :
        stmts
                             if-else
    if con :
       stmts1
    else:
       stmts2
       
    if con1:
    elif (con-1):
       stmts-1
    elif(con-2)
        stmts-1
    elif
    
    
     if con :
        if con be:
            stmts1
        else:
            stmts2
    else:
       stmts3
    
                        Loops :
        while-->
        for loop
    while BE :
       stmts
    else:
       stms
    range() : It is a function which can be geneate the values for the given
    range, In Ascending Order as well It will diplay the elmements in 
    descending 
       
        
                       

'''
list = [8,44,55,66,12,34,56,"abc",True,False]
a = 8
print(a in list)
print(a is not list)